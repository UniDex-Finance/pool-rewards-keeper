copy over env and fill in the values

The script works by reading all the from the graphql json, calls the contract for their current balance, and then disperses the native gas token (can be setup to be an erc20) to everyone in the pool based on their current balance.
Setup the keeper to run the script with a cron job or something similar, and you're good to go. The total reward will define how much gets rewarded to all poolers based on their share each time the script is ran.